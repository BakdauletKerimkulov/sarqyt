Приведи проект в соответствие с обновлённым ai_toolkit (code-style.md, riverpod.md, architecture.md). Мы сверили тулкит с эталонным проектом Андреа Бизотто (flutter-firebase-masterclass/ecommerce_app) и добавили в доки несколько решений — теперь код sarqyt должен им соответствовать.

Что нужно сделать:

1. Линтеры. Сейчас analysis_options.yaml стоковый — это плохо. Подключи custom_lint + riverpod_lint, исключи сгенерированные файлы (*.g.dart, *.freezed.dart) из анализа, добавь mocktail в dev-зависимости. После подключения прогони `dart run custom_lint` и исправь всё, что он найдёт (или явно покажи мне список, если правки спорные).

2. NotifierMounted. Создай миксин в lib/src/utils/notifier_mounted.dart (образец в riverpod.md) и переведи на него все контроллеры, где сейчас копипаста `_mounted` через try/catch. Заодно замени try/catch → AsyncError на `AsyncValue.guard`, кроме мест, где реально ветвимся по типу исключения.

3. Injectable clock. Создай currentDateBuilder-провайдер (riverpod.md → Injectable Clock) и замени прямые вызовы DateTime.now() в контроллерах, сервисах и доменной логике на него. У нас вся логика на pickup-окнах и датах офферов — я хочу мочь замораживать время в тестах. DateTime.now() в чисто отображенческом коде можно не трогать.

4. Robot-тесты. Заведи инфраструктуру по образцу из architecture.md → Robot pattern: test/src/robot.dart (композитный, поднимает приложение на фейках), пофичевые роботы. Для начала покрой самый критичный флоу: клиент открывает оффер → резервирует → видит заказ в списке. Не нужно покрывать всё — нужен работающий каркас + один сценарий, дальше будем наращивать.

5. Golden-тесты. dart_test.yaml с тегом golden, один golden-тест на главный экран (discover) в двух размерах — телефон и планшет. Это заодно проверит наши новые breakpoints из code-style.md.

6. Сверь остальное по мелочи: AsyncErrorLogger должен быть зарегистрирован в bootstrap (кажется, уже есть — проверь), ResponsiveCenter должен брать maxContentWidth из общего класса Breakpoints, а не из своей константы.

Порядок важен: сначала линтеры (п.1) — они подсветят проблемы для п.2. Пункты 2-3 — механический рефакторинг, после каждого прогоняй все тесты. Пункты 4-5 — новая инфраструктура, делай последними.

Что НЕ трогаем: envied пока не внедряем (нет острой нужды, .env-секретов на клиенте мало), integration_test/ на реальном Firebase не делаем.

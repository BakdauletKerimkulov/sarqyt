---
title: Admin Page Navigation
status: draft
date: 2026-07-05
type: feature
---

Добавить фичу, в которой условный админ может навигировать в любую страницу (пока только для бизнеса). Это нужно для того чтобы переходить на страницы и выявлять ошибку. Например, у меня ошибка в онбординге, не работает кнопка continue на экране review деталей (это реальная ошибка). В общем надо придумать как это решить или же создать тестовый способ проверки, в котором данные фейковые или что-то в этом роде. Придумай эталонный вариант

---

## Проверка реализации (2026-08-02)

Уже реализовано — это дубль `ai_specs/archive/013-admin-dev-menu-spec.md` (`status: done`, архивирован 2026-06-26), который закрывает ровно этот запрос:

- `lib/src/features/dev_menu/presentation/dev_menu_screen.dart` — полноэкранный список всех `BusinessRoute` (40+ пунктов) с навигацией по тапу.
- `lib/src/routing/business_router.dart:282-283` — маршрут `/dev` (`BusinessRoute.dev`); `query`-параметр `devMenu=true` (строка 138) снимает redirect-гарды, включая онбординг-экраны типа review-details из исходного бага.
- `lib/src/routing/scaffold_with_nested_navigation.dart:108-111` — `FloatingActionButton` виден только при `userRoleProvider == UserRole.admin`.

**Дальнейшие действия:** этот файл можно перенести в `archive/` как дубль 013 без отдельной спеки/плана (сам файл — request-only, не проходил `/spec`). Если у пользователя всё ещё воспроизводится баг с кнопкой continue на review-details — это отдельный `/fix` поверх уже работающего dev-меню, не новая фича навигации.

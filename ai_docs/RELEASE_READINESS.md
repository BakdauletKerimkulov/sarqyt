# Готовность к релизу

Снимок состояния на **2026-08-06**. Документ живой: закрываете пункт — вычёркиваете здесь.

Контекст: приложение **никогда не публиковалось** (`applicationId` = `com.example.sarqyt`), продовых пользователей нет, в Firestore только dev-данные. Это означает, что все необратимые решения (идентификаторы, регион, название) сейчас бесплатны, а после публикации — нет.

---

## Сводка

| Область | Готовность |
|---|---|
| Ядро: discover → бронь → выдача → отзыв | 90% |
| Бизнес-консоль | 65% — финансы и аналитика заглушки |
| Cloud Functions | 85% — логика есть, тесты вне CI |
| Security rules | 80% — написаны хорошо, в CI не проверяются |
| Конфиги релиза | 20% |
| Требования сторов | 40% |
| Локализация | 85% |
| Наблюдаемость | 40% — Crashlytics есть, аналитики нет |

Функционально MVP собран. До публикации в Google Play — порядка 2–3 недель, и почти вся работа не про фичи.

Целевая платформа — **Android + веб-консоль для бизнеса**. iOS отложен: нет аккаунта разработчика.

---

## Блокеры публикации в Google Play

1. **`applicationId = "com.example.sarqyt"`** (`android/app/build.gradle.kts:24`). Play отклоняет `com.example.*`. После первой заливки ID неизменяем.
2. **Release-сборка подписана debug-ключом** (`build.gradle.kts:36-39`). Нужен upload keystore.
3. **Удаление аккаунта — нерабочая заглушка.** `app_settings_screen.dart:51` показывает диалог без действия. `AuthRepository.deleteAccount()` реализован, но не вызывается; данные не чистятся. Требование User Data policy Google Play для приложений с регистрацией. Подтверждено фактом: в Firestore 6 документов `users` при 3 аккаунтах в Auth — три осиротевших профиля.
4. **Сборка по умолчанию собирает не то приложение.** `flutter build` без `--target` берёт `lib/main.dart` — это бизнес-приложение. Клиентское — `main_client.dart`. Нужен явный target в скриптах и CI. (Веб-хостинг при этом собирает бизнес-приложение корректно — там дефолт совпадает с задуманным.)
5. **Название продукта.** См. раздел «Бренд» ниже — блокирует пункты 1 и 2, потому что ID выводится из домена.

---

## Серьёзное, но не блокирующее публикацию

- **Бэкенд-тесты не проверяются в CI.** `ci.yml` для functions запускает только `lint` + `build`. 43 rules-теста и 13 интеграционных требуют эмулятор и в CI не гоняются. При этом `deploy.yml` автоматически деплоит `functions,firestore,storage` в прод при мёрдже в `main` — то есть правила безопасности уезжают непроверенными.
- **Golden-тесты протухли** после коммита с поиском (`853cc39`): 2 падения, диффы ~0.08%. CI их не ловит — `flutter test --exclude-tags golden`.
- **Оплаты нет.** Бронь без предоплаты, расчёт на терминале заведения. Риск no-show целиком на заведении: собрали бокс, клиент не пришёл. Прямых механизмов монетизации нет. Конкурент берёт оплату в приложении. Kaspi Pay стоит считать требованием к запуску, а не пунктом бэклога.
- **Заглушки в UI:** `financials_screen.dart` и `performance_screen.dart` — «Coming soon»; 11 точек `showNotImplementedAlertDialog` (весь Help Centre, тумблер пушей с захардкоженным `value: true`, часть favorites).
- **Нет продуктовой аналитики.** Crashlytics подключён (`app_bootstrap.dart:33-39`), но воронки и конверсии не видно.
- **Рассинхрон локализации:** `app_en.arb` — 396 ключей, `ru`/`kk` — по 368. 28 строк без перевода при том, что рынок казахстанский. Плюс 76 мест с `.hardcoded`.
- **`storage.rules` без лимитов** — нет ограничений на размер и content-type загружаемых файлов.
- **`StoreRole` не проверяется на сервере.** `assertStoreAccess` смотрит только на существование `storeShips`; `operator` и `employer` фактически равны `owner`. Ролевое разграничение — только в UI.
- **TTL-политика `storeDrafts`** не заведена и не деплоится через `firebase deploy`.
- **Закон о локализации персданных РК.** Хранение ПДн граждан РК за пределами Казахстана — вопрос к юристу, переездом в ЕС не решается. Может повлиять на выбор облака целиком.

---

## Принятые решения

**Регион.** Firestore фактически в `asia-south1` (не в `nam5`, как записано в `firebase.json`), Storage — в `us-central1`, callables — в `us-central1`. Замеры `time_total` из Казахстана: `europe-west1` 0.433 c, `europe-central2` 0.451 c, `us-central1` 0.582 c, `asia-south1` **1.193 c**. База стоит в худшем для своих пользователей регионе. Решение — переезд всего в `europe-west1` новым проектом. Планы: `ai_specs/archive/046-prepare-region-migration-plan.md` (подготовка, не зависела от названия, выполнена 2026-08-16) и `ai_specs/047-migrate-firebase-region-europe-west1-plan.md` (сам переезд, заблокирован названием).

**Платежи.** Интеграции нет и не будет без отдельной спеки. Stripe удалён полностью (код, зависимость, документация) — он не поддерживает KZT.

**Бренд.** Название меняется, см. ниже.

---

## Бренд: почему меняем

Проверено 2026-08-06:

- **`kz.sarqyt.app` — живой листинг в Google Play**, обновлён **17 июля 2026**, разработчик **Alfarabi Uzak**, установок «5+». В поиске Play не виден из-за малого числа установок — поэтому его легко не заметить.
- **`sarqyt.com`** зарегистрирован **20 мая 2026**, хостинг ps.kz, на домене живая админка с логином (`/login`, nginx).
- Описание в листинге: те же surprise-bag'и («Sarqyt-boxes»), те же −66%, те же рестораны, кафе и пекарни. Модель совпадает полностью, включая мерчант-панель.
- `sarqyt.kz` зарегистрирован 2026-07-29 **третьим лицом**, к конкуренту отношения не имеет, не резолвится.

Вывод: это не заброшенный проект 2021 года, а **активный конкурент**, зарелизившийся три недели назад. Package name в Play резервируется навсегда — `kz.sarqyt.app` недоступен никогда. Домен и имя фактически заняты.

При этом у конкурента 5+ установок, то есть тяги нет ни у кого. Гонка не проиграна: в двусторонних маркетплейсах выигрывает тот, кто первым наберёт плотность предложения в одном районе, а не тот, кто раньше зарелизился. Текущее состояние базы — **2 заведения, 4 позиции**.

**Не проверено:** подан ли товарный знак «Sarqyt» в QazPatent; сколько заведений реально подписал конкурент.

### Новое название

Выбрано предварительно: **Qorzhyn** (Қоржын) — переметная сума для припасов, буквально «сумка с едой».

Проверено свободно: `qorzhyn.kz`, `qorzhyn.com`, `kz.qorzhyn.app`.

Рассматривались также: `Sybaga` (Сыбаға — доля, отложенная для дорогого человека; смысл сильнее, но `.kz` занят и слово «на поверхности» — высокий риск повторной коллизии), `Qapshyq`, `Asqorap`, `Qutty`, `Qutqar`.

**Статус: не финальное.** Требует обсуждения с партнёром. До подтверждения не выполняются: переименование пакета, пересоздание Firebase-проекта, покупка доменов.

Перед фиксацией проверить: QazPatent, свободен ли инстаграм-хендл, отсутствие нежелательных сленговых значений (whois этого не покажет).

### Схема идентификаторов после смены имени

| Что | Значение |
|---|---|
| Клиентское приложение | `kz.qorzhyn.app` |
| Бизнес-приложение | `kz.qorzhyn.business` |
| Firebase prod | `qorzhyn-prod` |
| Firebase dev | существующий `sarqyt-1ab95` |
| Домен | `qorzhyn.kz` + `qorzhyn.com`, плюс варианты написания `korzhyn`/`qorjyn` в довесок |

Юр. страницы (условия, политика) переехать должны на свой домен: сейчас ссылки в `app_settings_screen.dart` ведут на `sarqyt-1ab95.web.app`, то есть на будущую песочницу.

### Цена переименования

243 файла с `package:sarqyt/`, 207 прочих вхождений в коде и конфигах, по 4 строки в каждом `.arb`, иконки и splash. Полдня, почти всё проверяется `flutter analyze` и тестами. Дёшево **только пока не опубликовано**.

---

## Уже сделано

- Stripe удалён полностью: зависимость `stripe@^21` из `functions/package.json` и lock-файла, раздел в `EXTERNAL_SERVICES.md`, упоминания в `PROJECT.md`, `FIRESTORE_GOTCHAS.md`, `CLOUD_FUNCTIONS_GOTCHAS.md`. `npm run lint` и `npm run build` зелёные. Осталось одно упоминание в `ai_toolkit/architecture.md` (пример имени переменной, правка заблокирована правами) и в архивных спеках — там это исторический протокол, трогать не нужно.
- Инвентаризация продового Firestore (Phase 0 плана миграции): данных для переноса нет.
- Создан пустой Firebase-проект `sarqyt-prod` — **будет удалён при смене имени**.

---

## Открытые вопросы к обсуждению

1. Название: **Qorzhyn** или другое. Товарный знак проверен?
2. Оплата: Kaspi Pay до запуска или запускаемся на брони без предоплаты, принимая риск no-show?
3. Локализация ПДн РК — нужен ответ юриста до публичного запуска.
4. Приоритет: сколько инженерных недель до того, как переключиться на подписание заведений. Сейчас в базе 2 заведения — это, а не техническая готовность, ограничивает запуск.

---

## См. также

- `ai_specs/archive/046-prepare-region-migration-plan.md` — подготовка к переезду: централизация региона callables, эмулятор в CI, лимиты Storage, `timeZone` у расписаний. Не зависела от названия, **выполнена 2026-08-16**
- `ai_specs/047-migrate-firebase-region-europe-west1-plan.md` — сам переезд, Phase 0 выполнена; заблокирован до утверждения названия
- `ai_docs/PROJECT.md` — обзор проекта и схема данных
- `ai_docs/BUSINESS_RULES.md` — доменные правила и известные пробелы в enforcement
